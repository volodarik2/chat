module.exports=function (req){
  /*var PHPUnserialize = require('php-unserialize');
  var user= req.headers.cookie.toString().split('=')[1] ; 
  user =  decodeURIComponent(user); 
  user=PHPUnserialize.unserialize(user);
  return user; */
  //console.log (req.cookies.tobi);

  return true;
}